import { Lexend, Mulish, Quicksand, Wix_Madefor_Display } from 'next/font/google'

export const lexend = Lexend({
  weight: ['500'],
  subsets: ['latin'],
})

export const raleway = Mulish({
  subsets: ['latin'],
})

export const quicksand = Wix_Madefor_Display({
  subsets: ['latin'],
})
